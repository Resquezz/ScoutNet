# Local development (copy to .env.local)
VITE_API_URL=http://localhost:5000/api

# Production build (Azure / GitHub Actions — set as repository variable or secret)
# VITE_API_URL=https://your-api.azurewebsites.net/api
